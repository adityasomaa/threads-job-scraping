# Threads credentials for scraping
THREADS_USERNAME=your_threads_username
THREADS_PASSWORD=your_threads_password

# Optional: how many posts per keyword to scrape
SCRAPE_MAX_POSTS=30

# Optional: run browser visibly (true) or headless (false)
HEADFUL=true
